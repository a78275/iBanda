-> instalar dremio
-> abrir no browser com o link dado
-> add sample source - acrescenta ficheiros como dados
-> dá para adicionar o PostgreSQL como camada inferior
-> importar a BD do PostgreSQL do tpcc que está a correr
-> em Jobs temos a história das interrogações feitas, e tem o explain
-> utilização das vistas materializadas - definir MV em Data, guardando a querie e ligando a materilização (nas definições -> reflections)
-> ParquetScan -> scan da vista materializada



WORKLOAD

=> cada cliente está ligado a só 1 warehouse, essa ligação é feita quando o cliente inicializa
=> número de clientes por warehouse -> conta até este número e põe todos a apontar para o primeiro warehouse
-> no caso de tpcc.number.warehouses=4 e clients=2 e tpcc.numclients=2 -> os dois clientes vão estar ligados ao warehouse 1, e com o SI vão fazer rollback
-> clients = tpcc.number.warehouses * tpcc.numclients (é apenas preciso mudar tpcc.number.warehouses e tpcc.numclients, clients é atualizado automaticamente)
-> 500=50*10 <- 500 clientes é impossível => think.time = true